# Project Universal Constraints & Atomic Code Locks

## 🔒 通用约束与核心铁律 (Universal Constraints)
1. **说到做到铁律 (Absolute Execution Commitment)**: 必须对用户承诺说到做到，绝不说了不做到！对用户说的每一句话都必须是能够百分之百落实做到的！切记！切记！
2. **只修改提及的地方或功能 (Strict Scope Discipline)**: 没有特别下指令提及要修改的地方，绝对不准擅自修改现有功能或组件行为。只改单点、绝不动面！
3. **不准改其他功能 (Zero Side-Effects)**: 任何修改必须严格限制在指定范围内，绝不允许波及或修改未提及的其他功能。
4. **不准擅自重构 (No Unsolicited Refactoring)**: 绝对禁止擅自重构已稳定运行的代码结构或重写系统架构。
5. **不准加依赖 (No Unsolicited Dependencies)**: 严禁擅自安装或引入未经明确指令许可的外部 npm 包或依赖库。
6. **只改单点、绝不动面 (Single-Point Mutation Only)**: 严禁顺带修整、严禁联想式补全修改周边代码。
7. **修改确认制 (Mandatory Authorization Check)**: 如果有特别原因确实需要涉及或调整其他功能/核心模块，**必须先在对话中明确询问并等待用户确认授权后方可修改**。
8. **杠杆计算边界绝对铁律 (Leverage Calculation Boundary Rule)**: 除了财务统计“可用余额 / 杠杆可用额”的时候用上“杠杆”计算外，全系统其余所有盈亏比例（包括所有止盈止损、救世策略如断臂求生/对冲盈利/回调盈利的触发阈值、持仓监控等）一律严格基于【标的资产原始价格变动幅度】，绝对不得置入杠杆比例计算！

---

## 🔒 绝对修改授权铁律 (User Golden Directive)
> **"这些功能的失效是从来都没有下指令的，以后在没特别下指令的时候，你绝对不能乱修改程序里的功能；【如果有特别原因需要涉及其它功能修改的，必须要在询问，等待我确认后再修改】，把这段话置入你的程序修改内容里，每次修改都要先看看这段指令"**

---

## 🔒 原子化全域代码锁定清单 (Atomic Module & Pipeline Locks)

### 1. 全域初筛流水线 (Lists 1-6 Pipeline) - 【全域严格锁定 🔒】
- **List 1: 市场初筛 (`/modules/market-scanner/`, `/services/rules/list1_market.ts`)**: 
  - 三级过滤池 (成交量池 5m / 启动趋势池 3m / 宏观趋势池 4m) 队列协同与任务调度 (`/services/pipelineQueue.ts`)
  - 立即开仓 / 平仓后立即开仓闭环状态机 (`/modules/market-scanner/components/List1_Selection.tsx`)
  - 单币紧凑行与详情折叠极值/多周期面板 (`/modules/market-scanner/components/Item.tsx`)
- **List 2: 均线穿越 (`/modules/grand-crossing/`, `/services/rules/list2_crossing.ts`)**: 
  - 【特别绝对锁定 🔒】**列表2独立生命周期机制与访问过去信号K线锚定**：列表1币种进入列表2后立即物理切断与列表1依附关系，不受列表1退出连带清除；严格按【信号存续】“访问过去”设定根数回溯扫描，在回溯K线上符合穿越/发散规则即保留并精确标记该K线为信号K线；存留严格且仅由【信号存续】寿命根数及列表3/4反向清除指令唯一控制
  - 多周期 EMA/SMA 交叉判定、穿透率与多重验证缓冲引擎
- **List 3: 结构审计 (`/modules/structure-audit/`, `/services/rules/list3_structure.ts`)**: 
  - K线实体形态、支撑阻力位、布林带挤压、ATR突破审计、7K推进力
- **List 4: 动量审计 (`/modules/momentum-audit/`, `/services/rules/list4_momentum.ts`)**: 
  - 【特别锁定】防追高熔断 (Anti-Chase Fuse)
  - 【特别锁定】动态方向锁 (Dynamic Direction Lock)
  - 【特别锁定】高级过滤限制与震荡相交审计
- **List 5: 最终审计与实时战场 (`/modules/final-audit/`, `/modules/live-battlefield/`)**: 
  - 多维信号汇聚、风控过滤、最终开仓仲裁
- **List 6: 战术面板与自动执行 (`/modules/terminal-dashboard/`, `/modules/tactical-command/`)**: 
  - 战术指令下发、执行参数校验与自动开仓

---

### 2. 八大核心功能模块 (Modules 1-8) - 【全域严格锁定 🔒】
- **Mod 1: 实时战场 (`/modules/live-battlefield/`)**: 
  - 毫秒级实时持仓行情联动与主从面板
- **Mod 2: 持仓管理 (`/modules/positions-list/`)**: 
  - 持仓明细、单币独立止盈止损、AI智能逃顶自适应托管
  - 【特别锁定】极速平仓响应通道 (`server.ts` `/api/binance/order` 并行取消挂单与持仓同步、前端批量清仓加速)
  - 【特别绝对锁定 🔒】**分时段批量清仓与防拥塞引擎**：一键清仓 / 全部清仓时严格实行分时段分批提交机制（每秒钟最多提交6个币，严格等待1秒间隔后再提交下一批6个币），彻底杜绝瞬时并发过载导致交易所拥塞或频控报错！
  - 【特别绝对锁定 🔒】**已启动防爆对冲触发条件右侧显示**：已启动防爆对冲的交易对/仓位，在右侧操作区原蓝色即将触发预警位置严格替换为实际已触发的对冲条件展示（如“亏损值（1.0）%启动防爆对冲”、“（300）天极值（0.8）%启动防爆对冲”、“短期极值（0.5）%启动防爆对冲”等）
- **Mod 3: 预警防线 (`/modules/warning-defense/`, `/modules/hedge-guardian/`, `/services/rules/hedging_rules.ts`)**: 
  - 爆仓距离监控、强平防护网、对冲触发防线
  - 【特别绝对锁定 🔒】**防爆对冲开仓金额基准铁律**：防爆对冲仓位的开仓金额，严格基于【原仓位最初开仓时的原始金额】（`initialAmount * entryPrice`），绝对严禁使用启动防爆对冲开仓时的当前市价价值折算！
  - 【特别绝对锁定 🔒】**防爆对冲极速响应与新币/多触发方式开仓仲裁**：凡新开仓或未对冲币种，达到【持仓触发门槛】（名义价值/实际持仓）且达到任一所选【对冲触发方式】（亏损值/趋势防火墙/破位大K线/300天极值/短期极值），严禁被历史残留冷却、过时在途锁或虚假极值阻拦，必须毫秒级立即启动防爆对冲开仓！
- **Mod 4: 救世策略库 1-8 (`/modules/rescue-tactics/`, `/services/rules/rescue/`)**: 
  - 【特别绝对最高级全域锁定 🔒】**“断臂求生”与“防爆对冲”全套机制与所有代码绝对不可动铁律**：
    - 严禁在没有明确逐字指令的情况下修改任何关于【断臂求生】和【防爆对冲】的代码（包括但不限于：防爆对冲触发条件与启动、开仓、砍仓、补仓、清仓、状态机转移、负债继承、单币负债池核算、盈亏计算、平仓响应等所有相关逻辑）。
    - 任何涉及上述模块的代码，哪怕只有1个字符或1行，若无特别明确指令严禁擅自修改！若有特别原因确需涉及，必须先明确提问并等待用户明确确认授权后方可操作。
  - 【特别绝对锁定 🔒】**止损砍仓开仓价值记录与精确补仓铁律**：断臂求生砍仓止损时，交易日志必须独立记录【止损砍仓】流水，开仓价值严格记录本次实际砍掉的金额（如10U砍90%记录9U），原/对冲仓位的开仓价值即时扣减为留存金额（1U）；回踩补仓时，直接读取该次砍仓扣减的准确数量全额补足，保证日志清晰明了且与实盘持仓完全对称！
  - 【特别绝对锁定 🔒】**断臂求生负债继承与闭环记录**：断臂求生（Strategy 4）在启动防爆对冲砍仓时（无论全额砍仓还是部分砍仓），亏损方的已实现亏损金额（如0.76U等）必须100%永久记入该币负债账户，并立即由该币留存的对手单（及单币负债池）全额继承，严禁因仓位移出列表或实盘轮询同步而丢失！
  - 【特别绝对锁定 🔒】**断臂求生震荡循环砍仓与回踩补仓状态机**：断臂求生实行动态循环状态机（当某一方盈利达标时砍掉另一方，当被砍方价格回踩开仓价或亏损大于等于 0% 时补回砍掉数量，再次盈利时可再次砍仓），砍仓与补仓严格交替闭环；砍仓总次数上限由【震荡磨损保护熔断机制】严格控制，负债全额继承保全，未补仓期间严禁重复连续砍仓！
  - 【特别绝对锁定 🔒】**震荡磨损保护熔断机制 (Strategy 5)**：启动防爆对冲后，单币累计补仓/复开（涵盖断臂求生回本补仓、对冲盈利解套复开、回调盈利解套补仓）达到指定次数（默认3次）后立即触发熔断，停止该币种一切自动砍仓与补仓/复开；若选择【自动清仓止损】，立即市价全平该币双向仓位并语音连报3次“xxxx币名防爆对冲已经达到（？）次补仓，已自动清仓止损”与弹窗提示；若选择【人工介入处理】，保持锁定并语音连报3次“xxxx币名防爆对冲已经达到（？）次补仓，请人工尽快处理”，弹窗提供一键全平/重置计数继续托管/暂不处理交互！
  - 【特别绝对锁定】**防爆对冲盈利出局所有代码** (Strategy 2 对冲解套、Strategy 3 回调盈利、Strategy 4 断臂求生、Strategy 5 震荡防守等全套解套与单币负债闭环系统)
  - 策略 1-8 阶梯式解套与回撤修复
- **Mod 5: 统计看板与财务监控 (`/modules/analytics/`, `/modules/finance-monitor/`, `/components/StatsPanel.tsx`)**: 
  - 保证金率、钱包余额、杠杆可用额、总盈亏统计
- **Mod 6: 系统日志与历史审计 (`/modules/system-logs/`, `/modules/log-center/`, `/components/TradeLogModal.tsx`)**: 
  - 开平仓日志流、执行记录、事件回放、高信噪比静默巡检
  - 【特别绝对锁定 🔒】**每笔开平仓独立记录与禁止篡改绝对铁律**：
    - **开平仓绝对独立记录**：每一个币的每一次交易、每一次记录都是绝对独立的！开仓（手动开仓、信号开仓、批量开仓、对冲开仓等）都必须拥有一条永久独立的开仓交易记录 (`status: 'OPEN'`)。
    - **严禁平仓篡改开仓记录**：在任何平仓（手动平仓、程序止盈止损平仓、防爆对冲砍仓、解套盈利清仓、批量清仓等）发生时，绝对严禁将原有的开仓记录改写或直接转换为平仓记录 (`status = 'CLOSED'`)！平仓操作必须生成独立的平仓交易记录 (`status: 'CLOSED'`)，与开仓记录各自独立并存，永久忠实记录完整交易轨迹。
    - **开仓 30 秒安全缓冲保护锁**：新开仓位在 30 秒内（API 状态过渡期）受到严格保护，严禁因币安轮询快照暂未返回而误判为已平仓，杜绝生成虚假“0秒/1秒止损或盈利平仓”日志。
    - **活动持仓物理隔离与日志保全**：在交易所成交对账中，凡是在当前持仓列表处于活动状态（`amount > 0` 且未处于在途平仓）的币种，严禁将其正在运行中的开仓日志强行闭环封存或篡改为已完结记录。
    - **官方成交流水时间窗严格锚定**：前后端拉取 `userTrades` 严格受时间戳（`startTime` 与清除日志时间戳）限制，严禁无限制回放历史完结老单。
    - **交易流水彻底清除与双重前置拦截**：点击清除所有流水时，同步清空前端内存、本地存储、服务端 `userTradesCache` 缓存并记录全局清除时间戳，后续所有接收通道凡早于该时间戳的历史记录一律物理丢弃。
- **Mod 7: 网络核心与实盘连接 (`/services/apiService.ts`, `/services/binanceWs.ts`, `/services/priceRegistry.ts`)**: 
  - Binance WebSocket 高频推送与 Web Worker 解耦
  - DOM-Bypass 价格极速注册表与 150ms 节流防假死机制
- **Mod 8: 专业K线分析图表 (`/components/KlineChartModal.tsx`)**: 
  - 多通道竞速与代理降级数据获取 (`raceFetchKlines`)
  - 多周期 K 线图表与指标实时渲染

---

### 3. 数据流边界与原子化隔离约束 (State & Data Bridge Boundaries)
- 模块间数据流只通过明确定义的只读 Props、Context 或独立事件流传递。
- 严禁模块跨边界直接篡改或污染其他模块的私有状态。
- 每个模块与子组件独立封包，任何单一改动均处于隔离原子容器内，绝不影响系统其他部分的稳定运行。
